<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service Confirmation | Professional Services</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-color: #0E3646;
            --primary-light: #1a5570;
            --primary-dark: #08252f;
            --accent-color: #e67e22;
            --success-color: #27ae60;
            --error-color: #e74c3c;
            --warning-color: #f39c12;
            --info-color: #3498db;
            --text-dark: #2c3e50;
            --text-light: #7f8c8d;
            --bg-light: #f8f9fa;
            --border-color: #dee2e6;
            --white: #ffffff;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            padding: 20px;
            line-height: 1.6;
            color: var(--text-dark);
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: var(--white);
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(14, 54, 70, 0.15);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-light) 100%);
            color: var(--white);
            padding: 40px 30px;
            text-align: center;
        }

        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .header p {
            font-size: 1.1em;
            opacity: 0.95;
        }

        .main-content {
            padding: 30px;
        }

        .confirmation-grid {
            display: grid;
            grid-template-columns: 1fr 400px;
            gap: 30px;
        }

        .form-section {
            background: var(--white);
        }

        .section-card {
            background: var(--bg-light);
            border-radius: 8px;
            padding: 25px;
            margin-bottom: 25px;
            border-left: 4px solid var(--primary-color);
        }

        .section-card h3 {
            color: var(--primary-color);
            font-size: 1.4em;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .section-card p {
            color: var(--text-light);
            margin-bottom: 20px;
        }

        .order-reference {
            background: var(--white);
            padding: 20px;
            border-radius: 8px;
            border: 2px solid var(--primary-color);
            margin-bottom: 20px;
        }

        .order-details {
            display: none;
            background: #e8f4fd;
            padding: 20px;
            border-radius: 8px;
            margin-top: 15px;
        }

        .order-details.show {
            display: block;
        }

        .detail-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
        }

        .detail-item {
            display: flex;
            flex-direction: column;
        }

        .detail-label {
            font-weight: 600;
            color: var(--text-dark);
            font-size: 0.85em;
            margin-bottom: 5px;
        }

        .detail-value {
            color: var(--text-light);
            font-size: 0.95em;
        }

        .service-summary-table {
            width: 100%;
            background: var(--white);
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
        }

        .service-summary-table table {
            width: 100%;
            border-collapse: collapse;
        }

        .service-summary-table thead {
            background: var(--primary-color);
            color: var(--white);
        }

        .service-summary-table th {
            padding: 15px;
            text-align: left;
            font-weight: 600;
            font-size: 0.95em;
        }

        .service-summary-table td {
            padding: 15px;
            border-bottom: 1px solid var(--border-color);
        }

        .service-summary-table tbody tr:hover {
            background: var(--bg-light);
        }

        .service-summary-table tbody tr:last-child td {
            border-bottom: none;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--primary-color);
            font-size: 0.95em;
        }

        label .required {
            color: var(--error-color);
            margin-left: 3px;
        }

        label .optional {
            color: var(--text-light);
            font-weight: 400;
            font-style: italic;
            font-size: 0.9em;
        }

        input[type="text"],
        input[type="email"],
        input[type="tel"],
        input[type="date"],
        select,
        textarea {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid var(--border-color);
            border-radius: 6px;
            font-size: 1em;
            transition: all 0.3s ease;
            font-family: inherit;
            background: var(--white);
        }

        input:focus,
        select:focus,
        textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(14, 54, 70, 0.1);
        }

        textarea {
            resize: vertical;
            min-height: 100px;
        }

        .checkbox-group {
            display: flex;
            align-items: center;
            padding: 12px;
            border: 2px solid var(--border-color);
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-bottom: 10px;
        }

        .checkbox-group:hover {
            border-color: var(--primary-color);
            background: var(--bg-light);
        }

        .checkbox-group input[type="checkbox"] {
            width: 20px;
            height: 20px;
            margin-right: 10px;
            cursor: pointer;
            accent-color: var(--primary-color);
        }

        .checkbox-group label {
            margin: 0;
            cursor: pointer;
            font-weight: 500;
        }

        .radio-group {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
            margin-top: 10px;
        }

        .radio-option {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .radio-option input[type="radio"] {
            width: 18px;
            height: 18px;
            cursor: pointer;
            accent-color: var(--primary-color);
        }

        .radio-option label {
            margin: 0;
            cursor: pointer;
            font-weight: 500;
        }

        .signature-pad-container {
            background: var(--white);
            border: 2px solid var(--border-color);
            border-radius: 8px;
            padding: 15px;
            margin-top: 15px;
        }

        .signature-canvas {
            width: 100%;
            height: 200px;
            border: 2px dashed var(--border-color);
            border-radius: 6px;
            cursor: crosshair;
            background: var(--white);
        }

        .signature-actions {
            display: flex;
            gap: 10px;
            margin-top: 10px;
        }

        .signature-btn {
            padding: 8px 16px;
            border: none;
            border-radius: 6px;
            font-size: 0.9em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn-clear {
            background: var(--error-color);
            color: var(--white);
        }

        .btn-clear:hover {
            background: #c0392b;
        }

        .info-box {
            background: #e8f4fd;
            border-left: 4px solid var(--info-color);
            padding: 15px 20px;
            border-radius: 6px;
            margin-bottom: 20px;
        }

        .info-box p {
            color: var(--text-dark);
            margin: 0;
            line-height: 1.6;
        }

        .warning-box {
            background: #fff3cd;
            border-left: 4px solid var(--warning-color);
            padding: 15px 20px;
            border-radius: 6px;
            margin-bottom: 20px;
        }

        .warning-box p {
            color: var(--text-dark);
            margin: 0;
            line-height: 1.6;
        }

        .success-box {
            background: #d4edda;
            border-left: 4px solid var(--success-color);
            padding: 15px 20px;
            border-radius: 6px;
            margin-bottom: 20px;
        }

        .success-box p {
            color: var(--text-dark);
            margin: 0;
            line-height: 1.6;
        }

        .confirmation-summary {
            background: var(--bg-light);
            padding: 25px;
            border-radius: 8px;
            height: fit-content;
            position: sticky;
            top: 20px;
        }

        .confirmation-summary h2 {
            color: var(--primary-color);
            margin-bottom: 20px;
            font-size: 1.5em;
            border-bottom: 2px solid var(--primary-color);
            padding-bottom: 10px;
        }

        .summary-section {
            margin-bottom: 20px;
        }

        .summary-section h4 {
            color: var(--primary-color);
            font-size: 1.1em;
            margin-bottom: 10px;
        }

        .summary-item {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid var(--border-color);
        }

        .summary-item:last-child {
            border-bottom: none;
        }

        .summary-item .label {
            color: var(--text-light);
            font-weight: 500;
        }

        .summary-item .value {
            color: var(--text-dark);
            font-weight: 600;
        }

        .status-badge {
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 0.9em;
            font-weight: 600;
            text-align: center;
            margin-top: 10px;
        }

        .status-pending {
            background: #fff3cd;
            color: #856404;
        }

        .status-confirmed {
            background: #d4edda;
            color: #155724;
        }

        .status-rejected {
            background: #f8d7da;
            color: #721c24;
        }

        .button-group {
            display: flex;
            gap: 15px;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .btn {
            padding: 15px 40px;
            border: none;
            border-radius: 6px;
            font-size: 1.1em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            flex: 1;
            min-width: 150px;
        }

        .btn-primary {
            background: var(--primary-color);
            color: var(--white);
        }

        .btn-primary:hover {
            background: var(--primary-light);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(14, 54, 70, 0.3);
        }

        .btn-secondary {
            background: var(--white);
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
        }

        .btn-secondary:hover {
            background: var(--bg-light);
        }

        .btn-success {
            background: var(--success-color);
            color: var(--white);
        }

        .btn-success:hover {
            background: #229954;
            transform: translateY(-2px);
        }

        .btn-danger {
            background: var(--error-color);
            color: var(--white);
        }

        .btn-danger:hover {
            background: #c0392b;
        }

        .btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1000;
            align-items: center;
            justify-content: center;
            overflow-y: auto;
            padding: 20px;
        }

        .modal.show {
            display: flex;
        }

        .modal-content {
            background: var(--white);
            padding: 40px;
            border-radius: 12px;
            max-width: 800px;
            width: 100%;
            max-height: 90vh;
            overflow-y: auto;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.3s ease;
        }

        @keyframes slideIn {
            from {
                transform: translateY(-50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .modal-icon {
            font-size: 4em;
            margin-bottom: 20px;
            text-align: center;
        }

        .success-icon {
            color: var(--success-color);
        }

        .modal-content h2 {
            color: var(--primary-color);
            margin-bottom: 15px;
            text-align: center;
        }

        .modal-content p {
            color: var(--text-light);
            margin-bottom: 20px;
            line-height: 1.6;
            text-align: center;
        }

        .confirmation-number {
            background: var(--bg-light);
            padding: 15px;
            border-radius: 6px;
            font-weight: 600;
            color: var(--primary-color);
            margin: 20px 0;
            font-size: 1.2em;
            text-align: center;
        }

        .confirmation-certificate {
            background: var(--white);
            border: 3px solid var(--primary-color);
            border-radius: 12px;
            padding: 40px;
            margin: 30px 0;
            text-align: center;
        }

        .certificate-header {
            margin-bottom: 30px;
        }

        .certificate-header h3 {
            color: var(--primary-color);
            font-size: 2em;
            margin-bottom: 10px;
        }

        .certificate-body {
            margin: 30px 0;
            line-height: 2;
        }

        .certificate-signature-area {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-top: 40px;
        }

        .signature-block {
            text-align: center;
        }

        .signature-line {
            border-top: 2px solid var(--text-dark);
            margin: 10px 0;
            padding-top: 5px;
        }

        .error-message {
            color: var(--error-color);
            font-size: 0.9em;
            margin-top: 5px;
            display: none;
        }

        .error-message.show {
            display: block;
        }

        .input-error {
            border-color: var(--error-color) !important;
        }

        .loading-spinner {
            display: none;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255, 255, 255, 0.3);
            border-top-color: var(--white);
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
            margin-left: 10px;
            display: inline-block;
            vertical-align: middle;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .btn-success.loading .loading-spinner {
            display: inline-block;
        }

        @media (max-width: 1200px) {
            .confirmation-grid {
                grid-template-columns: 1fr;
            }

            .confirmation-summary {
                position: static;
                order: -1;
            }
        }

        @media (max-width: 768px) {
            .header h1 {
                font-size: 2em;
            }

            .form-grid {
                grid-template-columns: 1fr;
            }

            .detail-grid {
                grid-template-columns: 1fr;
            }

            .button-group {
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }

            .certificate-signature-area {
                grid-template-columns: 1fr;
            }

            .service-summary-table {
                overflow-x: auto;
            }
        }

        @media print {
            body {
                background: var(--white);
                padding: 0;
            }

            .header,
            .button-group,
            .confirmation-summary,
            .section-card p {
                display: none;
            }

            .modal-content {
                box-shadow: none;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Service Confirmation</h1>
            <p>Confirm service order completion and customer satisfaction</p>
        </div>

        <div class="main-content">
            <div class="confirmation-grid">
                <div class="form-section">
                    <form id="confirmationForm">
                        <!-- Service Order Reference -->
                        <div class="section-card">
                            <h3>📋 Service Order Reference</h3>
                            <p>Select a completed service order to confirm</p>

                            <div class="info-box">
                                <p><strong>💡 Note:</strong> Only completed service orders are available for confirmation. This document serves as proof of service delivery and customer acceptance.</p>
                            </div>

                            <div class="order-reference">
                                <div class="form-group">
                                    <label for="orderSelect">Select Completed Service Order <span class="required">*</span></label>
                                    <select id="orderSelect" required>
                                        <option value="">-- Select Service Order --</option>
                                    </select>
                                </div>

                                <input type="hidden" id="selectedOrderId">

                                <div class="order-details" id="orderDetails"></div>
                            </div>
                        </div>

                        <!-- Service Summary -->
                        <div class="section-card" id="serviceSummarySection" style="display: none;">
                            <h3>📝 Service Summary</h3>
                            <p>Review the services provided</p>

                            <div class="service-summary-table">
                                <table>
                                    <thead>
                                        <tr>
                                            <th>Service Description</th>
                                            <th>Quantity</th>
                                            <th>Unit Price</th>
                                            <th>Total</th>
                                        </tr>
                                    </thead>
                                    <tbody id="serviceSummaryBody">
                                        <!-- Service items will be populated here -->
                                    </tbody>
                                    <tfoot>
                                        <tr style="background: var(--bg-light); font-weight: bold;">
                                            <td colspan="3" style="text-align: right; padding: 15px;">Total Amount:</td>
                                            <td id="totalAmount" style="padding: 15px; color: var(--primary-color); font-size: 1.2em;">$0.00</td>
                                        </tr>
                                    </tfoot>
                                </table>
                            </div>
                        </div>

                        <!-- Customer Information -->
                        <div class="section-card" id="customerInfoSection" style="display: none;">
                            <h3>👤 Customer Verification</h3>
                            <p>Verify customer identity and contact information</p>

                            <div class="form-grid">
                                <div class="form-group">
                                    <label for="customerName">Customer Name <span class="required">*</span></label>
                                    <input type="text" id="customerName" placeholder="Full name" required>
                                    <div class="error-message" id="customerNameError">Customer name is required</div>
                                </div>
                                <div class="form-group">
                                    <label for="customerEmail">Email Address <span class="required">*</span></label>
                                    <input type="email" id="customerEmail" placeholder="customer@example.com" required>
                                    <div class="error-message" id="customerEmailError">Valid email is required</div>
                                </div>
                                <div class="form-group">
                                    <label for="customerPhone">Phone Number <span class="required">*</span></label>
                                    <input type="tel" id="customerPhone" placeholder="+1 (555) 123-4567" required>
                                    <div class="error-message" id="customerPhoneError">Phone number is required</div>
                                </div>
                                <div class="form-group">
                                    <label for="confirmationDate">Confirmation Date <span class="required">*</span></label>
                                    <input type="date" id="confirmationDate" required>
                                    <div class="error-message" id="confirmationDateError">Confirmation date is required</div>
                                </div>
                            </div>
                        </div>

                        <!-- Service Acceptance -->
                        <div class="section-card" id="serviceAcceptanceSection" style="display: none;">
                            <h3>✅ Service Acceptance</h3>
                            <p>Customer confirmation of service completion</p>

                            <div class="warning-box">
                                <p><strong>⚠️ Important:</strong> By confirming below, the customer acknowledges that all services have been completed satisfactorily.</p>
                            </div>

                            <div style="margin-top: 20px;">
                                <h4 style="color: var(--primary-color); margin-bottom: 15px;">Acceptance Checklist</h4>

                                <div class="checkbox-group">
                                    <input type="checkbox" id="check1" class="acceptance-check" required>
                                    <label for="check1">I confirm that all services listed were completed as specified in the service order</label>
                                </div>

                                <div class="checkbox-group">
                                    <input type="checkbox" id="check2" class="acceptance-check" required>
                                    <label for="check2">I confirm that the work quality meets the agreed standards</label>
                                </div>

                                <div class="checkbox-group">
                                    <input type="checkbox" id="check3" class="acceptance-check" required>
                                    <label for="check3">I confirm that all materials used were as specified or approved</label>
                                </div>

                                <div class="checkbox-group">
                                    <input type="checkbox" id="check4" class="acceptance-check" required>
                                    <label for="check4">I confirm that the service location was left clean and tidy</label>
                                </div>

                                <div class="checkbox-group">
                                    <input type="checkbox" id="check5" class="acceptance-check" required>
                                    <label for="check5">I have received all necessary documentation, warranties, and instructions</label>
                                </div>
                            </div>

                            <div class="form-group full-width" style="margin-top: 25px;">
                                <label>Overall Satisfaction Rating <span class="required">*</span></label>
                                <div class="radio-group">
                                    <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
                                        <input type="radio" name="satisfaction" value="5" required> ⭐⭐⭐⭐⭐ Excellent
                                    </label>
                                    <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
                                        <input type="radio" name="satisfaction" value="4"> ⭐⭐⭐⭐ Good
                                    </label>
                                    <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
                                        <input type="radio" name="satisfaction" value="3"> ⭐⭐⭐ Average
                                    </label>
                                    <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
                                        <input type="radio" name="satisfaction" value="2"> ⭐⭐ Poor
                                    </label>
                                    <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
                                        <input type="radio" name="satisfaction" value="1"> ⭐ Very Poor
                                    </label>
                                </div>
                                <div class="error-message" id="satisfactionError">Please select a satisfaction rating</div>
                            </div>

                            <div class="form-group full-width">
                                <label for="customerFeedback">Customer Feedback <span class="optional">(Optional)</span></label>
                                <textarea id="customerFeedback" placeholder="Any comments, concerns, or suggestions..."></textarea>
                            </div>
                        </div>

                        <!-- Digital Signature -->
                        <div class="section-card" id="signatureSection" style="display: none;">
                            <h3>✍️ Customer Signature</h3>
                            <p>Digital signature to confirm service acceptance</p>

                            <div class="signature-pad-container">
                                <label style="margin-bottom: 10px;">Sign below to confirm <span class="required">*</span></label>
                                <canvas id="signatureCanvas" class="signature-canvas"></canvas>
                                <div class="signature-actions">
                                    <button type="button" class="signature-btn btn-clear" onclick="clearSignature()">Clear Signature</button>
                                </div>
                                <div class="error-message" id="signatureError">Signature is required</div>
                            </div>

                            <div class="form-group full-width" style="margin-top: 20px;">
                                <label for="signerName">Printed Name <span class="required">*</span></label>
                                <input type="text" id="signerName" placeholder="Name of person signing" required>
                                <div class="error-message" id="signerNameError">Signer name is required</div>
                            </div>

                            <div class="form-group full-width">
                                <label for="signerTitle">Title/Relationship <span class="optional">(Optional)</span></label>
                                <input type="text" id="signerTitle" placeholder="e.g., Property Owner, Facility Manager, Authorized Representative">
                            </div>
                        </div>

                        <!-- Additional Information -->
                        <div class="section-card" id="additionalInfoSection" style="display: none;">
                            <h3>📌 Additional Information</h3>
                            <p>Any additional notes or special conditions</p>

                            <div class="form-group full-width">
                                <div class="checkbox-group">
                                    <input type="checkbox" id="warrantyProvided">
                                    <label for="warrantyProvided">Warranty documentation provided to customer</label>
                                </div>
                            </div>

                            <div class="form-group full-width">
                                <div class="checkbox-group">
                                    <input type="checkbox" id="followUpRequired">
                                    <label for="followUpRequired">Follow-up visit required</label>
                                </div>
                            </div>

                            <div class="form-group full-width" id="followUpDetails" style="display: none;">
                                <label for="followUpNotes">Follow-up Details</label>
                                <textarea id="followUpNotes" placeholder="Describe the reason for follow-up and when it should occur..."></textarea>
                            </div>

                            <div class="form-group full-width">
                                <label for="additionalNotes">Additional Notes <span class="optional">(Optional)</span></label>
                                <textarea id="additionalNotes" placeholder="Any special conditions, agreements, or observations..."></textarea>
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="button-group" id="actionButtons" style="display: none;">
                            <button type="button" class="btn btn-secondary" onclick="resetForm()">Cancel</button>
                            <button type="button" class="btn btn-danger" onclick="rejectService()">Reject Service</button>
                            <button type="submit" class="btn btn-success" id="confirmBtn">
                                Confirm Service Completion
                                <span class="loading-spinner"></span>
                            </button>
                        </div>
                    </form>
                </div>

                <!-- Confirmation Summary Sidebar -->
                <div class="confirmation-summary">
                    <h2>Confirmation Summary</h2>
                    <div id="summaryContent">
                        <p style="text-align: center; color: var(--text-light); padding: 20px;">Select a completed service order to begin</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Success Modal -->
    <div class="modal" id="successModal">
        <div class="modal-content">
            <div class="modal-icon success-icon">✓</div>
            <h2>Service Confirmed Successfully!</h2>
            <p>The service has been officially confirmed and accepted by the customer.</p>
            <div class="confirmation-number">
                Confirmation Number: <span id="confirmationNumberDisplay"></span>
            </div>

            <div class="confirmation-certificate">
                <div class="certificate-header">
                    <h3>SERVICE COMPLETION CERTIFICATE</h3>
                    <p>This certifies that the following service has been completed and accepted</p>
                </div>

                <div class="certificate-body" id="certificateBody">
                    <!-- Certificate content will be populated here -->
                </div>

                <div class="certificate-signature-area">
                    <div class="signature-block">
                        <div class="signature-line" id="customerSignatureDisplay"></div>
                        <p><strong>Customer Signature</strong></p>
                        <p id="customerNameDisplay"></p>
                        <p id="confirmationDateDisplay"></p>
                    </div>
                    <div class="signature-block">
                        <div class="signature-line">Professional Services Co.</div>
                        <p><strong>Service Provider</strong></p>
                        <p>Authorized Representative</p>
                        <p id="currentDateDisplay"></p>
                    </div>
                </div>
            </div>

            <p style="font-size: 0.95em;">A copy of this confirmation certificate has been sent to the customer's email address.</p>
            
            <div class="button-group" style="justify-content: center;">
                <button class="btn btn-secondary" onclick="window.print()">Print Certificate</button>
                <button class="btn btn-primary" onclick="closeSuccessModal()">Done</button>
            </div>
        </div>
    </div>

    <!-- Rejection Modal -->
    <div class="modal" id="rejectionModal">
        <div class="modal-content" style="max-width: 600px;">
            <div class="modal-icon" style="color: var(--error-color);">✗</div>
            <h2>Service Rejection</h2>
            <p>Please provide a reason for rejecting this service completion</p>

            <div class="form-group full-width">
                <label for="rejectionReason">Reason for Rejection <span class="required">*</span></label>
                <textarea id="rejectionReason" placeholder="Explain why the service is being rejected and what needs to be corrected..." required style="min-height: 120px;"></textarea>
            </div>

            <div class="button-group">
                <button class="btn btn-secondary" onclick="closeRejectionModal()">Cancel</button>
                <button class="btn btn-danger" onclick="submitRejection()">Submit Rejection</button>
            </div>
        </div>
    </div>

    <script>
        // Mock completed service orders database
        const completedOrders = [
            {
                id: 'ORD-20240001',
                orderNumber: 'ORD-20240001',
                customer: 'John Smith',
                email: 'john.smith@example.com',
                phone: '+1 (555) 123-4567',
                serviceType: 'HVAC Maintenance',
                location: '123 Main St, Downtown',
                completionDate: '2024-02-10',
                scheduledDate: '2024-02-10',
                technicians: ['David Rodriguez', 'Lisa Anderson'],
                totalAmount: 450.00,
                services: [
                    { description: 'HVAC System Inspection', quantity: 1, unitPrice: 150.00 },
                    { description: 'Filter Replacement', quantity: 2, unitPrice: 50.00 },
                    { description: 'System Cleaning', quantity: 1, unitPrice: 200.00 }
                ]
            },
            {
                id: 'ORD-20240002',
                orderNumber: 'ORD-20240002',
                customer: 'ABC Corporation',
                email: 'facilities@abccorp.com',
                phone: '+1 (555) 234-5678',
                serviceType: 'Electrical Installation',
                location: '456 Business Park, Suite 200',
                completionDate: '2024-02-11',
                scheduledDate: '2024-02-11',
                technicians: ['Robert Kim', 'Amanda White'],
                totalAmount: 1250.00,
                services: [
                    { description: 'Electrical Panel Installation', quantity: 1, unitPrice: 800.00 },
                    { description: 'Wiring & Cable Management', quantity: 1, unitPrice: 350.00 },
                    { description: 'Safety Inspection & Testing', quantity: 1, unitPrice: 100.00 }
                ]
            },
            {
                id: 'ORD-20240003',
                orderNumber: 'ORD-20240003',
                customer: 'Sarah Johnson',
                email: 'sarah.j@email.com',
                phone: '+1 (555) 345-6789',
                serviceType: 'Plumbing Repair',
                location: '789 Residential Ave, Apt 5B',
                completionDate: '2024-02-09',
                scheduledDate: '2024-02-09',
                technicians: ['David Rodriguez'],
                totalAmount: 325.00,
                services: [
                    { description: 'Leak Repair - Kitchen Sink', quantity: 1, unitPrice: 175.00 },
                    { description: 'Pipe Replacement', quantity: 1, unitPrice: 100.00 },
                    { description: 'Water Pressure Check', quantity: 1, unitPrice: 50.00 }
                ]
            },
            {
                id: 'ORD-20240004',
                orderNumber: 'ORD-20240004',
                customer: 'Tech Solutions Inc',
                email: 'it@techsolutions.com',
                phone: '+1 (555) 456-7890',
                serviceType: 'Network Installation',
                location: '321 Tech Plaza, Floor 3',
                completionDate: '2024-02-12',
                scheduledDate: '2024-02-12',
                technicians: ['Robert Kim', 'Amanda White', 'Lisa Anderson'],
                totalAmount: 2100.00,
                services: [
                    { description: 'Network Infrastructure Setup', quantity: 1, unitPrice: 1200.00 },
                    { description: 'Cabling & Hardware Installation', quantity: 1, unitPrice: 600.00 },
                    { description: 'Configuration & Testing', quantity: 1, unitPrice: 300.00 }
                ]
            }
        ];

        // Order search functionality
        const orderSelect = document.getElementById('orderSelect');
        const orderDetails = document.getElementById('orderDetails');
        const selectedOrderId = document.getElementById('selectedOrderId');

        // Populate dropdown with completed orders
        function initializeOrderSelect() {
            completedOrders.forEach(order => {
                const option = document.createElement('option');
                option.value = order.id;
                option.textContent = `${order.orderNumber} - ${order.customer} - ${order.serviceType} ($${order.totalAmount.toFixed(2)})`;
                orderSelect.appendChild(option);
            });
        }

        orderSelect.addEventListener('change', function() {
            const orderId = this.value;
            if (orderId) {
                selectOrder(orderId);
            } else {
                // Hide sections if no order selected
                orderDetails.classList.remove('show');
                document.getElementById('serviceSummarySection').style.display = 'none';
                document.getElementById('customerInfoSection').style.display = 'none';
                document.getElementById('serviceAcceptanceSection').style.display = 'none';
                document.getElementById('signatureSection').style.display = 'none';
                document.getElementById('additionalInfoSection').style.display = 'none';
                document.getElementById('actionButtons').style.display = 'none';
                updateSummary();
            }
        });

        function selectOrder(orderId) {
            const order = completedOrders.find(o => o.id === orderId);
            if (!order) return;

            selectedOrderId.value = orderId;

            // Display order details
            orderDetails.innerHTML = `
                <h4 style="color: var(--primary-color); margin-bottom: 15px;">✓ Selected Service Order</h4>
                <div class="detail-grid">
                    <div class="detail-item">
                        <span class="detail-label">Order Number:</span>
                        <span class="detail-value">${order.orderNumber}</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Customer:</span>
                        <span class="detail-value">${order.customer}</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Service Type:</span>
                        <span class="detail-value">${order.serviceType}</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Location:</span>
                        <span class="detail-value">${order.location}</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Scheduled Date:</span>
                        <span class="detail-value">${order.scheduledDate}</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Completion Date:</span>
                        <span class="detail-value">${order.completionDate}</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Technicians:</span>
                        <span class="detail-value">${order.technicians.join(', ')}</span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Total Amount:</span>
                        <span class="detail-value" style="color: var(--primary-color); font-weight: 700;">$${order.totalAmount.toFixed(2)}</span>
                    </div>
                </div>
            `;
            orderDetails.classList.add('show');

            // Populate service summary table
            const summaryBody = document.getElementById('serviceSummaryBody');
            summaryBody.innerHTML = order.services.map(service => `
                <tr>
                    <td>${service.description}</td>
                    <td>${service.quantity}</td>
                    <td>$${service.unitPrice.toFixed(2)}</td>
                    <td>$${(service.quantity * service.unitPrice).toFixed(2)}</td>
                </tr>
            `).join('');
            document.getElementById('totalAmount').textContent = `$${order.totalAmount.toFixed(2)}`;

            // Auto-populate customer information
            document.getElementById('customerName').value = order.customer;
            document.getElementById('customerEmail').value = order.email;
            document.getElementById('customerPhone').value = order.phone;

            // Show all sections
            document.getElementById('serviceSummarySection').style.display = 'block';
            document.getElementById('customerInfoSection').style.display = 'block';
            document.getElementById('serviceAcceptanceSection').style.display = 'block';
            document.getElementById('signatureSection').style.display = 'block';
            document.getElementById('additionalInfoSection').style.display = 'block';
            document.getElementById('actionButtons').style.display = 'flex';

            updateSummary();
        }

        // Close suggestions when clicking outside
        document.addEventListener('click', function(e) {
            if (!orderSearchInput.contains(e.target) && !orderSuggestions.contains(e.target)) {
                orderSuggestions.classList.remove('show');
            }
        });

        // Signature pad functionality
        const canvas = document.getElementById('signatureCanvas');
        const ctx = canvas.getContext('2d');
        let isDrawing = false;
        let hasSignature = false;

        // Set canvas size
        function resizeCanvas() {
            const rect = canvas.getBoundingClientRect();
            canvas.width = rect.width;
            canvas.height = rect.height;
        }
        resizeCanvas();
        window.addEventListener('resize', resizeCanvas);

        canvas.addEventListener('mousedown', startDrawing);
        canvas.addEventListener('mousemove', draw);
        canvas.addEventListener('mouseup', stopDrawing);
        canvas.addEventListener('mouseout', stopDrawing);

        // Touch events for mobile
        canvas.addEventListener('touchstart', (e) => {
            e.preventDefault();
            const touch = e.touches[0];
            const mouseEvent = new MouseEvent('mousedown', {
                clientX: touch.clientX,
                clientY: touch.clientY
            });
            canvas.dispatchEvent(mouseEvent);
        });

        canvas.addEventListener('touchmove', (e) => {
            e.preventDefault();
            const touch = e.touches[0];
            const mouseEvent = new MouseEvent('mousemove', {
                clientX: touch.clientX,
                clientY: touch.clientY
            });
            canvas.dispatchEvent(mouseEvent);
        });

        canvas.addEventListener('touchend', (e) => {
            e.preventDefault();
            const mouseEvent = new MouseEvent('mouseup', {});
            canvas.dispatchEvent(mouseEvent);
        });

        function startDrawing(e) {
            isDrawing = true;
            hasSignature = true;
            const rect = canvas.getBoundingClientRect();
            ctx.beginPath();
            ctx.moveTo(e.clientX - rect.left, e.clientY - rect.top);
        }

        function draw(e) {
            if (!isDrawing) return;
            const rect = canvas.getBoundingClientRect();
            ctx.lineTo(e.clientX - rect.left, e.clientY - rect.top);
            ctx.strokeStyle = '#000';
            ctx.lineWidth = 2;
            ctx.lineCap = 'round';
            ctx.stroke();
        }

        function stopDrawing() {
            isDrawing = false;
        }

        function clearSignature() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            hasSignature = false;
        }

        // Follow-up toggle
        document.getElementById('followUpRequired').addEventListener('change', function() {
            document.getElementById('followUpDetails').style.display = this.checked ? 'block' : 'none';
        });

        // Update summary
        function updateSummary() {
            const summaryContent = document.getElementById('summaryContent');
            const orderId = selectedOrderId.value;

            if (!orderId) {
                summaryContent.innerHTML = '<p style="text-align: center; color: var(--text-light); padding: 20px;">Select a completed service order to begin</p>';
                return;
            }

            const order = completedOrders.find(o => o.id === orderId);
            let html = '';

            html += '<div class="summary-section">';
            html += '<h4>Order Details</h4>';
            html += `<div class="summary-item"><span class="label">Order:</span><span class="value">${order.orderNumber}</span></div>`;
            html += `<div class="summary-item"><span class="label">Customer:</span><span class="value">${order.customer}</span></div>`;
            html += `<div class="summary-item"><span class="label">Service:</span><span class="value">${order.serviceType}</span></div>`;
            html += '</div>';

            html += '<div class="summary-section">';
            html += '<h4>Amount</h4>';
            html += `<div class="summary-item"><span class="label">Total:</span><span class="value" style="color: var(--primary-color); font-size: 1.2em;">$${order.totalAmount.toFixed(2)}</span></div>`;
            html += '</div>';

            // Check acceptance status
            const allChecked = Array.from(document.querySelectorAll('.acceptance-check')).every(cb => cb.checked);
            const satisfactionSelected = document.querySelector('input[name="satisfaction"]:checked');
            
            if (allChecked && satisfactionSelected && hasSignature) {
                html += '<div class="status-badge status-confirmed">Ready to Confirm</div>';
            } else {
                html += '<div class="status-badge status-pending">Pending Confirmation</div>';
            }

            summaryContent.innerHTML = html;
        }

        // Monitor changes
        document.querySelectorAll('.acceptance-check').forEach(cb => {
            cb.addEventListener('change', updateSummary);
        });

        document.querySelectorAll('input[name="satisfaction"]').forEach(radio => {
            radio.addEventListener('change', updateSummary);
        });

        // Rejection functionality
        function rejectService() {
            document.getElementById('rejectionModal').classList.add('show');
        }

        function closeRejectionModal() {
            document.getElementById('rejectionModal').classList.remove('show');
        }

        function submitRejection() {
            const reason = document.getElementById('rejectionReason').value.trim();
            if (!reason) {
                alert('Please provide a reason for rejection.');
                return;
            }

            alert('Service rejection has been recorded. The service provider will be notified to address the issues.');
            closeRejectionModal();
            location.reload();
        }

        // Form submission
        document.getElementById('confirmationForm').addEventListener('submit', function(e) {
            e.preventDefault();

            const orderId = selectedOrderId.value;
            if (!orderId) {
                alert('Please select a service order.');
                return;
            }

            // Validate all acceptance checks
            const allChecked = Array.from(document.querySelectorAll('.acceptance-check')).every(cb => cb.checked);
            if (!allChecked) {
                alert('Please check all acceptance items.');
                return;
            }

            // Validate satisfaction rating
            if (!document.querySelector('input[name="satisfaction"]:checked')) {
                document.getElementById('satisfactionError').classList.add('show');
                return;
            }

            // Validate signature
            if (!hasSignature) {
                document.getElementById('signatureError').classList.add('show');
                alert('Please provide a signature.');
                return;
            }

            // Validate signer name
            if (!document.getElementById('signerName').value.trim()) {
                document.getElementById('signerNameError').classList.add('show');
                return;
            }

            const confirmBtn = document.getElementById('confirmBtn');
            confirmBtn.classList.add('loading');
            confirmBtn.disabled = true;

            // Simulate confirmation processing
            setTimeout(() => {
                confirmBtn.classList.remove('loading');
                confirmBtn.disabled = false;

                const order = completedOrders.find(o => o.id === orderId);
                const confirmationNum = 'CONF-' + Date.now().toString().slice(-8);
                
                document.getElementById('confirmationNumberDisplay').textContent = confirmationNum;
                
                // Populate certificate
                const certificateBody = document.getElementById('certificateBody');
                certificateBody.innerHTML = `
                    <p><strong>Service Order:</strong> ${order.orderNumber}</p>
                    <p><strong>Service Type:</strong> ${order.serviceType}</p>
                    <p><strong>Service Location:</strong> ${order.location}</p>
                    <p><strong>Completion Date:</strong> ${order.completionDate}</p>
                    <p><strong>Total Amount:</strong> $${order.totalAmount.toFixed(2)}</p>
                    <p><strong>Satisfaction Rating:</strong> ${document.querySelector('input[name="satisfaction"]:checked').value} Stars</p>
                `;

                // Display signature
                const signatureDataUrl = canvas.toDataURL();
                document.getElementById('customerSignatureDisplay').innerHTML = `<img src="${signatureDataUrl}" alt="Signature" style="max-width: 100%; height: auto;">`;
                
                document.getElementById('customerNameDisplay').textContent = document.getElementById('customerName').value;
                document.getElementById('confirmationDateDisplay').textContent = document.getElementById('confirmationDate').value;
                document.getElementById('currentDateDisplay').textContent = new Date().toLocaleDateString();

                document.getElementById('successModal').classList.add('show');
            }, 2000);
        });

        function closeSuccessModal() {
            document.getElementById('successModal').classList.remove('show');
            location.reload();
        }

        // Reset form
        function resetForm() {
            if (confirm('Cancel confirmation? All entered data will be lost.')) {
                location.reload();
            }
        }

        // Set default confirmation date
        const today = new Date().toISOString().split('T')[0];
        document.getElementById('confirmationDate').value = today;

        // Initialize order dropdown
        initializeOrderSelect();
    </script>
</body>
</html>