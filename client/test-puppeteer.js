import puppeteer from 'puppeteer'; (async () => { try { const browser = await puppeteer.launch({ headless: 'new' }); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', error => console.error('PAGE ERROR:', error.message)); page.on('requestfailed', request => console.error('REQUEST FAILED:', request.url())); await page.goto('http://localhost:5173/inventory', { waitUntil: 'networkidle0' }); console.log('Navigation complete'); await browser.close(); } catch(e) { console.error('PUPPETEER ERROR', e); } })();
