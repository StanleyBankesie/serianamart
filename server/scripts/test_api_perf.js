async function test() { const res = await fetch('http://localhost:3000/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username: 'admin', password: 'admin' }) }); const data = await res.json(); const token = data.token; console.time('items'); await fetch('http://localhost:3000/api/inventory/items', { headers: { Authorization: Bearer  } }); console.timeEnd('items'); console.time('invoices'); await fetch('http://localhost:3000/api/sales/invoices', { headers: { Authorization: Bearer  } }); console.timeEnd('invoices'); process.exit(0); } test();
