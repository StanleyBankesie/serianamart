const mysql = require('mysql2/promise'); async function run() { const con = await mysql.createConnection({host:'localhost', user:'root', password:'', database:'demo_db'}); try { const [res] = await con.query(SELECT COUNT(*) AS count, created_at, u.username AS created_by_name FROM pur_orders LEFT JOIN adm_users u ON u.id = created_by); console.log(res); } catch(e) { console.error('Error:', e.message); } process.exit(0); } run().catch(console.error);
